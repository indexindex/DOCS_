// TODO: DATA TYPES

// * in JS we have 7 primitive types:

// *    Number
// *    String
// *    Boolean
// *    Null
// *    Undefined

// *    Symbol
// *    BigInt

// ? first 5 are main types, last two are less common



// TODO: TYPE OPERATOR

// * for each primitive type we can use "typeof" operator to check the type of our value
typeof 50;
// ? return value: "number"

typeof '50';
// ? return value: "string"

typeof true;
// ? return value: "boolean"

// * "typeof" operator always returns a string